spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-plain-block-alov3qz3-pooler.c-3.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_peOIyV31TufP}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jackson:
    deserialization:
      fail-on-null-for-primitives: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:kino.klara.notify@gmail.com}
    password: ${MAIL_PASSWORD:vzyaxfekaufvnkqn}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: ${JWT_SECRET:dGhpcklzQVZlcnlTZWN1cmVKd3RTZWNyZXRLZXlGb3JLbGFyYVRoZWF0cmVTeXN0ZW0h}
    expiration: 86400000
  upload:
    dir: ./uploads/shows
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}
